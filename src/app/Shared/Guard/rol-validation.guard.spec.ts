import { TestBed } from '@angular/core/testing';

import { RolValidationGuard } from './rol-validation.guard';
/*
describe('RolValidationGuard', () => {
  let guard: RolValidationGuard;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    guard = TestBed.inject(RolValidationGuard);
  });

  it('should be created', () => {
    expect(guard).toBeTruthy();
  });
});
*/